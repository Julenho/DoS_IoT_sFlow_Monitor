Influx

#wget -qO- https://repos.influxdata.com/influxdb.key | gpg --dearmor | tee /etc/apt/trusted.gpg.d/influxdb.gpg > /dev/null
#export DISTRIB_ID=$(lsb_release -si); export DISTRIB_CODENAME=$(lsb_release -sc)
#echo "deb [signed-by=/etc/apt/trusted.gpg.d/influxdb.gpg] https://repos.influxdata.com/${DISTRIB_ID,,} ${DISTRIB_CODENAME} stable" | tee /etc/apt/sources.list.d/influxdb.list > /dev/null#apt-get update && apt-get install influxdb2
#apt-get update && apt-get install influxdb2

wget https://dl.influxdata.com/influxdb/releases/influxdb2-2.2.0-linux-amd64.tar.gz
tar xvfz influxdb2-2.2.0-linux-amd64.tar.gz

FastNetMon

wget https://install.fastnetmon.com/installer -Oinstaller

chmod +x installer

./installer -install_community_edition

service fastnetmon restart


Grafana

wget -q -O - https://packages.grafana.com/gpg.key | apt-key add -

echo "deb https://packages.grafana.com/oss/deb stable main" | tee -a /etc/apt/sources.list.d/grafana.list

sudo service grafana-server restart

_____________________


Please login into http://your.ip.ad.dr.ess:81 or https://your.ip.ad.dr.ess:443 with login admin and password NuWxeLiFbade2295



